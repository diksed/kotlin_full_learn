<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Kotlin_full_learn"
        tools:targetApi="31">
        <activity
            android:name=".oop.OOPActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".catch_the_kenny.CatchTheKennyActivity"
            android:exported="true"></activity>
        <activity
            android:name=".stopwatch.StopwatchActivity"
            android:exported="true" />
        <activity
            android:name=".count_down.CountDownActivity"
            android:exported="true" />
        <activity
            android:name=".second_screen.SecondScreenActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true" />
    </application>

</manifest>